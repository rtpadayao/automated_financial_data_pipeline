project-root/
│
├── docker-compose.yml          # Orchestrates all services
├── Dockerfile                  # Optional root-level custom image (e.g. Airflow)
├── .env                        # Environment variables (DB creds, configs)
├── requirements.txt            # Python dependencies
├── .gitignore
├── README.md
│
├── postgres/                   # Initialization scripts
│   └── init.sql                # Schema + base table creation
│
├── flaskapi/                   # Custom API service
│   ├── Dockerfile              # Service-specific image
│   ├── app.py                  # Mock API serving GL data
│   ├── requirements.txt
│   └── data/
│       └── gl_transactions.csv # Sample dataset
│
├── dags/                       # Airflow DAGs
│   └── gl_pipeline.py          # Extract → Load → dbt run
│
├── dbt/                        # dbt project
│   ├── dbt_project.yml         # dbt project config
│   ├── profiles.yml            # Connection to Postgres
│   ├── models/                 # Transformations
│   │   ├── source.yml          # Source definitions
│   │   ├── transform.sql       # Incremental transformation
│   │   ├── test_amount.sql     # Data quality test
│   │   └── test_dup_entry.sql  # Duplicate entry test
│   ├── seeds/                  # Static CSVs for reference data
│   │   └── chart_of_accounts.csv
│   └── snapshots/              # Optional: slowly changing dimensions
│
├── airflow_logs/               # Persisted Airflow logs
└── postgres_data/              # Persisted Postgres data
